# Run every 2 minutes. Uses the current repo; no repo name is hardcoded.
# Add steps here to refresh API config or trigger deploy when needed.

name: Update API config

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update-api-config:
    runs-on: ubuntu-latest
    steps:
      - name: Update API config
        run: echo "API config check (add your steps here)"
