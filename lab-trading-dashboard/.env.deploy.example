ii
# Copy to .env and fill in so deploy/setup can run. Do NOT commit .env to git.
# Cloud Ubuntu (150.241.244.130) SSH password = DEPLOY_PASSWORD.
# DB server (150.241.245.36) SSH password = DB_SERVER_PASSWORD (if different). Postgres password on that server = set as DB_PASSWORD on cloud when using DB server.

DEPLOY_HOST=root@150.241.244.130
DEPLOY_PASSWORD=***

# DB server — for run-open-db-port-on-db-server, copy-database-to-cloud. Postgres password on 150.241.245.36 is set on cloud as DB_PASSWORD when pointing to DB server.
# DB_SERVER=root@150.241.245.36
# DB_SERVER_PASSWORD=***

# To copy FULL DB to cloud (so dashboard shows all trades), set the DB that has the data (e.g. 150.241.245.36 if reachable from your machine):
# FULL_DB_HOST=your-postgres-host
# FULL_DB_USER=postgres
# FULL_DB_PASSWORD=***
# FULL_DB_NAME=labdb2
# Then run: ./scripts/dump-and-restore-to-cloud.sh

REPO_URL=https://github.com/Loveleet/lab_live.git

# Action password (Auto-Pilot, Execute, End Trade, etc.) is read from DB table lab_settings.
# On the DB: INSERT INTO lab_settings (key, value) VALUES ('action_password', 'your_secret') ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value, updated_at = NOW();
