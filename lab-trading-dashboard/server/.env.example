# Optional: use cloud or remote PostgreSQL (no local DB needed)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/labdb2

# Or for local PostgreSQL (macOS often uses your username, not "postgres"):
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=apple
# DB_PASSWORD=
# DB_NAME=labdb2

# PORT=10000
