# When running on localhost (npm run dev), the app uses the CLOUD server for data by default.
# Copy to .env.local to override.

# If your network can't reach the cloud IP, set the tunnel URL (get it from cloud or Actions):
# VITE_API_BASE_URL=https://your-tunnel.trycloudflare.com

# To use a local Node server instead of cloud (e.g. server-local.js on port 3001):
# VITE_API_BASE_URL=http://localhost:3001
